Step1: Create a new .env file and then store your GROQ api key in the .env file with the name below
GROQ_API_KEY = ypur api key

Step2: Navigate to the correct directory and run below command
pip install -r requirements.txt

step3: Run python app.py if windows, if macos or linux run python3 app.py

